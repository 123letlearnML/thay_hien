@model mvc.Models.Diem

@{
    ViewData["Title"] = "Tính Điểm";
}

<div class="w3-container w3-margin">
    <div class="w3-card-4 w3-light-grey w3-text-blue">
        <div class="w3-container w3-blue">
            <h2 class="w3-center">Tính Tổng Điểm</h2>
        </div>
        <div class="w3-container w3-padding">
            <form method="post" asp-action="TinhDiem">
                <div class="w3-section">
                    <label for="DiemA" class="w3-text-blue">Điểm A:</label>
                    <input asp-for="DiemA" class="w3-input w3-border" />
                    <span asp-validation-for="DiemA" class="w3-text-red"></span>
                </div>
                <div class="w3-section">
                    <label for="DiemB" class="w3-text-blue">Điểm B:</label>
                    <input asp-for="DiemB" class="w3-input w3-border" />
                    <span asp-validation-for="DiemB" class="w3-text-red"></span>
                </div>
                <div class="w3-section">
                    <label for="DiemC" class="w3-text-blue">Điểm C:</label>
                    <input asp-for="DiemC" class="w3-input w3-border" />
                    <span asp-validation-for="DiemC" class="w3-text-red"></span>
                </div>
                <div class="w3-section w3-center">
                    <button type="submit" class="w3-button w3-blue w3-padding">🧮 Tính Điểm</button>
                </div>
            </form>
            @if (ViewBag.SumDiem != null)
            {
                <h2 class="w3-center w3-text-green">Kết Quả: @ViewBag.SumDiem</h2>
            }
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}