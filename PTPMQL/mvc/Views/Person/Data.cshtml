@model IEnumerable<mvc.Models.Person>

<div class="container mt-4">
    <div class="card shadow-lg">
        <div class="card-header bg-primary text-white text-center">
            <h2>Danh sách Person</h2>
        </div>
        <div class="card-body">
            <div class="mb-3">
                <a asp-action="Create" class="btn btn-success">➕ Thêm Person</a>
            </div>
            <table class="table table-striped table-bordered">
                <thead class="table-dark">
                    <tr>
                        <th>PersonId</th>
                        <th>FullName</th>
                        <th>Address</th>
                        <th class="text-center">Hành động</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.PersonId</td>
                            <td>@item.FullName</td>
                            <td>@item.Address</td>
                            <td class="text-center">
                                <a asp-action="Edit" asp-route-id="@item.PersonId" class="btn btn-warning">✏️ Sửa</a>
                                <a asp-action="Delete" asp-route-id="@item.PersonId" class="btn btn-danger">🗑️ Xóa</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>