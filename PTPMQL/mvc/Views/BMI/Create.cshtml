@model mvc.Models.BMI

<div class="w3-container w3-margin">
    <div class="w3-card-4 w3-light-grey w3-text-blue">
        <div class="w3-container w3-blue">
            <h2 class="w3-center">Tính chỉ số BMI</h2>
        </div>
        <div class="w3-container w3-padding">
            <form method="post" asp-action="Calculate">
                <div class="w3-section">
                    <label for="Weight" class="w3-text-blue">Cân nặng (kg):</label>
                    <input type="number" step="0.1" id="Weight" name="Weight" value="@Model?.Weight" class="w3-input w3-border" required />
                </div>
                <div class="w3-section">
                    <label for="Height" class="w3-text-blue">Chiều cao (m):</label>
                    <input type="number" step="0.01" id="Height" name="Height" value="@Model?.Height" class="w3-input w3-border" required />
                </div>
                <div class="w3-section w3-center">
                    <button type="submit" class="w3-button w3-blue w3-padding">📏 Tính toán</button>
                </div>
            </form>
            @if (ViewBag.Error != null)
            {
                <p class="w3-text-red w3-center">@ViewBag.Error</p>
            }
        </div>
    </div>
</div>